service: shared-photo-album

provider:
    name: aws
    runtime: nodejs10.x
    stage: dev

functions:
    graphql:
        handler: dist/graphql.handler
        events:
            - http:
                path: graphql
                method: GET
                cors: true
            - http:
                path: graphql
                method: POST
                cors: true

resources: